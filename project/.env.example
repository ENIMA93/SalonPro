# Supabase – use the same project as in Supabase Dashboard
# Get both values from: Dashboard → Settings → API

# Project URL (e.g. https://YOUR_PROJECT_REF.supabase.co)
VITE_SUPABASE_URL=https://ngqyfjhcxtimnebdqxen.supabase.co

# Anon public key only (long JWT starting with eyJ...). Do NOT use service_role in the frontend.
VITE_SUPABASE_ANON_KEY=your_anon_public_key_here

# Edge Function "create-staff-user" (used when admin adds staff with login) needs in Supabase:
# Dashboard → Edge Functions → create-staff-user → Secrets: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY.
# Never expose the service_role key in the browser.
